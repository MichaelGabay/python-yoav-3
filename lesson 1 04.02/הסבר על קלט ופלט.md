  <style>
      pre {
        text-align: left;
        direction: ltr;
      }
    </style>

# קלט ופלט בפייתון - Input and Output

## מה זה קלט ופלט?

**קלט (Input)** - מידע שהמשתמש מכניס לתוכנית  
**פלט (Output)** - מידע שהתוכנית מציגה למשתמש

בפייתון, אנו משתמשים בשתי פונקציות עיקריות:

- `input()` - לקריאת קלט מהמשתמש
- `print()` - להצגת פלט למשתמש

---

## הפונקציה `print()` - הצגת פלט

הפונקציה `print()` משמשת להצגת מידע על המסך.

### תחביר בסיסי:

```python
print("Hello, World!")
```

### דוגמאות:

```python
# הדפסת טקסט פשוט
print("Welcome to Python!")

# הדפסת מספרים
print(42)
print(3.14)

# הדפסת מספר משתנים
name = "Yoav"
age = 25
print(name, age)

# הדפסה עם f-string (מומלץ!)
print(f"Hello {name}, you are {age} years old")

# הדפסה עם מספר שורות
print("Line 1")
print("Line 2")
print("Line 3")
```

### פרמטרים שימושיים:

```python
# sep - מפריד בין ערכים (ברירת מחדל: רווח)
print("Hello", "World", sep="-")  # Output: Hello-World

# end - מה להדפיס בסוף (ברירת מחדל: שורה חדשה)
print("Hello", end=" ")
print("World")  # Output: Hello World
```

---

## הפונקציה `input()` - קריאת קלט

הפונקציה `input()` משמשת לקריאת מידע מהמשתמש.

### תחביר בסיסי:

```python
user_input = input("Enter your name: ")
```

### איך זה עובד:

1. התוכנית מציגה את ההודעה למשתמש
2. התוכנית ממתינה שהמשתמש יקליד משהו
3. המשתמש לוחץ Enter
4. הערך שהוקלד נשמר במשתנה

### דוגמאות:

```python
# קריאת טקסט
name = input("Enter your name: ")
print(f"Hello {name}!")

# קריאת מספר (חשוב להמיר!)
age = int(input("Enter your age: "))
print(f"You are {age} years old")

# קריאת מספר עשרוני
price = float(input("Enter the price: "))
print(f"The price is: {price}")
```

### ⚠️ חשוב לזכור:

**`input()` תמיד מחזירה מחרוזת (string)!**

אם אתה רוצה לעבוד עם מספרים, אתה חייב להמיר:

```python
# ❌ שגוי - לא יעבוד!
number = input("Enter a number: ")
result = number + 2  # זה יחבר מחרוזות, לא יחשב!

# ✅ נכון - המרה למספר
number = int(input("Enter a number: "))
result = number + 2  # עכשיו זה יעבוד!
```

### סוגי המרות:

```python
# המרה למספר שלם
age = int(input("Enter your age: "))

# המרה למספר עשרוני
height = float(input("Enter your height: "))

# המרה למחרוזת (לרוב לא נדרש, כי input כבר מחזיר string)
text = str(input("Enter text: "))
```

---

## דוגמאות משולבות

### דוגמה 1: תוכנית פשוטה

```python
name = input("What is your name? ")
print(f"Nice to meet you, {name}!")
```

**Output:**

```
What is your name? Yoav
Nice to meet you, Yoav!
```

### דוגמה 2: חישוב פשוט

```python
x = int(input("Enter a number: "))
print(f"Half of {x} is: {x / 2}")
```

**Output:**

```
Enter a number: 10
Half of 10 is: 5.0
```

### דוגמה 3: מספר קלטים

```python
first_name = input("Enter your first name: ")
last_name = input("Enter your last name: ")
age = int(input("Enter your age: "))

print(f"Full name: {first_name} {last_name}")
print(f"Age: {age}")
```

**Output:**

```
Enter your first name: Yoav
Enter your last name: Cohen
Enter your age: 25
Full name: Yoav Cohen
Age: 25
```

### דוגמה 4: חישוב מתמטי

```python
num1 = float(input("Enter first number: "))
num2 = float(input("Enter second number: "))

sum_result = num1 + num2
product = num1 * num2

print(f"Sum: {sum_result}")
print(f"Product: {product}")
```

**Output:**

```
Enter first number: 5
Enter second number: 3
Sum: 8.0
Product: 15.0
```

---

## טיפים חשובים

1. **תמיד המר מספרים** - `input()` מחזיר string, אז אם אתה צריך מספר, השתמש ב-`int()` או `float()`

2. **הודעות ברורות** - כתוב הודעות `input()` ברורות כדי שהמשתמש יבין מה להכניס:

   ```python
   # ✅ טוב
   age = int(input("Enter your age: "))

   # ❌ פחות טוב
   age = int(input("age: "))
   ```

3. **טיפול בשגיאות** - אם המשתמש מכניס משהו שלא מספר, התוכנית תקרוס. נלמד על זה בהמשך!

---

## סיכום

| פונקציה   | שימוש             | דוגמה                             |
| --------- | ----------------- | --------------------------------- |
| `print()` | הצגת פלט          | `print("Hello")`                  |
| `input()` | קריאת קלט         | `name = input("Name: ")`          |
| `int()`   | המרה למספר שלם    | `age = int(input("Age: "))`       |
| `float()` | המרה למספר עשרוני | `price = float(input("Price: "))` |

---

## תרגול

למדת את הבסיס! עכשיו תוכל לעבור לקובץ `תרגילים.md` ולפתור את התרגילים.

**בהצלחה! 🚀**
